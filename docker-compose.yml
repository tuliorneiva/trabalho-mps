services:
  postgres-dev:
    image: postgres:15
    container_name: trabalho-mps-postgres
    env_file: .env
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: trabalho-mps
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data


volumes:
  postgres_dev_data: